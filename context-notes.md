# 작업 노트

## 표제어 중복 검사 (2026-08-12)

### 왜 필요했나

정규화가 **생성 단계**에서 일어난다는 점이 문제의 뿌리다. 선택 화면에서 `societies`와 `society`는
서로 다른 문자열이라 아무 문제가 없어 보이지만, AI가 둘 다 `society`로 원형화하면
결과 표에 똑같은 줄이 두 개 생긴다. 사용자는 4분을 기다린 뒤에야 그걸 보게 된다.

기존에 있던 검사는 **위치 겹침**(`clash`)뿐이었다. 이건 "같은 자리를 두 번 고르는 것"을 막을 뿐,
다른 지문·다른 자리의 같은 단어는 그대로 통과시킨다.

### 굴절과 파생을 왜 나눴나

한 가지 기준으로 묶으면 쓸모가 없다.

- `society` / `societies` — 정규화하면 **확실히 같아진다**. 그냥 두면 결과가 망가진다.
- `economy` / `economic` — 뿌리는 같지만 **둘 다 표제어로 가치가 있을 수 있다**.

둘을 같은 무게로 경고하면 사용자는 경고를 무시하기 시작한다. 그래서 굴절만 떼는 키와
파생 접미사까지 떼는 키를 따로 만들고, 화면에서도 다르게 보여준다(밑줄 유무).

### MIN_STEM = 4 가 유일한 방어선

규칙 기반 어간 추출의 실패는 대부분 **너무 많이 자르는 것**으로 나타난다.

- `ration` → `ion` 을 떼면 `rat` — 전혀 다른 단어와 묶인다
- `less` → `less` 를 떼면 빈 문자열

그래서 접미사를 떼고 남는 길이가 4자 미만이면 아예 자르지 않는다. 이 규칙 하나로
`ration`/`rat`, `less`/`lesson`, `nation`/`nat` 오탐이 전부 사라진다.
반대로 미탐(못 잡는 것)은 감수한다 — 못 잡으면 예전과 같을 뿐이지만,
오탐은 사용자가 멀쩡한 표제어를 지우게 만든다.

### 끝소리 두 가지를 다듬는 이유

검증에서 `rely` / `reliable` 이 안 잡혀서 알게 된 것.

- `e` 제거 — `immerse` ↔ `immersion`
- `y` → `i` — 접미사가 붙으면 y가 i로 바뀐다. `rely` ↔ `reliable`, `rely` ↔ `reliance`

### AI 응답을 코드로 다시 막는 이유

`exclude` 목록을 프롬프트로 넘기고 있었지만 그건 **부탁**이다. 받은 응답에서 중복을 걸러내는
코드가 없었다. 개수 보장을 코드로 하고 있으면서(`MAX_ROUNDS` 재요청) 중복만 AI를 믿을 이유가 없다.
단, 여기서 막는 것은 **굴절형 중복까지만**이다. 파생형은 통과시키고 경고로 넘긴다 —
자동으로 빼면 오탐일 때 조용히 사라져서 사용자가 알아차릴 방법이 없다.

### 남은 것

**생성 후 중복은 아직 검사하지 않는다.** 선택 시점에는 파생형으로 보였던 둘을 AI가 같은 원형으로
정규화해버리면 결과 표에 중복이 남는다. 결과 표에서 `headword` 기준으로 한 번 더 보면 완전해진다.

## 1단계 화면이 건너뛰어지던 문제 (2026-08-12)

`loadPassages` 가 `step: passages.length ? 'select' : 'input'` 으로 지문을 만들자마자 화면을
2단계로 넘기고 있었다. 그래서 SetupPanel 의 **지문 미리보기 · 단어장 설정 · "표제어 선택 시작" 버튼**을
아무도 볼 수 없었다. 헤더의 ① 을 다시 눌러야만 나타났다.

세 덩어리의 UI 가 통째로 죽어 있었는데 화면이 넘어가는 게 자연스러워 보여서 드러나지 않았다.
README 에는 "나뉜 결과는 미리보기에서 확인하고… 여기서 총 표제어 개수, 선택 방식, AI 모델을 정한다"고
적혀 있어 의도한 흐름이 무엇이었는지는 분명했다. `step: 'input'` 으로 두어 버튼으로만 넘어가게 했다.

## 단어장 제목 (2026-08-12)

파일 이름 만들기와 엑셀 첫 줄 만들기를 **호출부가 아니라 `downloadXlsx` 안에서** 하도록 옮겼다.
호출부에서 기본 제목 문자열을 조립하면 그 문자열이 그대로 파일 이름이 되어
`정상JLS_심화단어장_—_샘플지문.docx_….xlsx` 같은 것이 나온다. 제목(사용자 입력)과
출처 이름(파일명)을 따로 받아야 둘을 다르게 쓸 수 있다.

제목이 특수문자만으로 이루어져 걷어내면 빈 문자열이 되는 경우가 있어, 그때는 기본 이름으로 되돌아간다.

## 단어장 불러오기 (2026-08-29)

### 왜 새 파이프라인이 필요 없나

`quiz-spec.md` 는 "입력 — 더 받지 않는다" 로 시작한다. `rows` 와 `passages` 가 이미 앱 안에
있다는 전제다. 그런데 시험지 코드를 실제로 읽어보면 **전제가 아니라 그냥 인자다.**
`allocate(rows)`, `buildParts(rows)` 모두 배열 하나만 받는다. `App.jsx` 의 `available.quiz` 도
`rows.length > 0` 이 전부다.

즉 불러오기는 "새 흐름을 만드는 일"이 아니라 **`rows` 를 다른 경로로 채우는 일**이다.
시험지 쪽 코드는 한 줄도 건드리지 않는다.

### surface 가 왜 핵심인가

XLSX 9열에서 빠진 것 중 나머지는 아쉬운 정도지만 `surface` 는 다르다.

```js
answer: row.surface,                          // quizBuild.js
form: verbForm(row.surface, row.headword),    // quizBuild.js
```

`verbForm` 은 `surface === headword` 면 무조건 `'base'` 를 돌려준다. 그래서 surface 를
headword 로 대체하면 PART IV 후보 5개가 전부 '원형'이 되고, `takeDiverseVerbs` 의
형태별 라운드로빈이 한 갈래만 돌게 된다. 명세의 "5문항의 형태가 서로 달라야 한다"가
조용히 죽는다. **에러가 나지 않고 결과만 나빠지는 종류의 손실**이라 더 위험하다.

### 왜 열을 늘리지 않고 숨김 시트로 가나

`surface`·`properNoun`·`passageNo` 를 보이는 시트에 열로 붙이면 왕복은 되지만,
선생님이 여는 파일에 뜻 모를 열이 셋 늘어난다. 이 파일은 출력해서 학생에게 주기도 하는 물건이다.
`state: 'veryHidden'` 으로 두면 엑셀 UI 에서 숨김 해제조차 안 되므로 실수로 건드릴 일이 없다.

`_meta` 를 JSON 문자열 한 칸에 넣는 방법도 있었지만 두 가지가 걸린다. 셀 32767자 제한과,
깨졌을 때 아무것도 알 수 없다는 점이다. 표 형태면 열어서 눈으로 확인할 수 있다.

### 역추적을 fallback 으로만 두는 이유

`_meta` 가 있으면 정확한 값이 있는데 굳이 추정할 이유가 없다. 역추적은 **이미 나가 있는
옛 파일**을 위한 것이다. `duplicates.js` 의 `inflectionKey` 를 재활용하면
`develop`/`developing` 은 잡히지만 `buy`/`bought` 같은 불규칙은 못 잡는다.

못 잡은 것을 headword 로 채우고 넘어가되 **개수를 화면에 알린다.** 중복 검사 때와 같은 판단이다 —
자동으로 처리하고 침묵하면 사용자가 결과가 나빠진 이유를 알 방법이 없다.

### 지문을 필수로 하지 않은 이유

`renderPartIVItem` 에 `passages` 가 없으면 이미 fallback 이 있다(문장 하나만 쓰고 40단어 검사).
명세도 "유추가 가능하면 붙이지 않는다"고 했으니 붙이지 않는 것이 기본에 가깝다.
지문 파일을 필수로 걸면 몇 달 전 단어장으로 시험지를 뽑으려는 사람이 아예 막힌다.
그게 이 기능을 만드는 이유인데 그걸 막으면 앞뒤가 안 맞는다.

### 무엇을 어느 시트의 진실로 볼 것인가

내보낸 XLSX 를 선생님이 엑셀에서 손으로 고치는 일이 있다. 그렇다면 **보이는 시트가 내용의 진실**이다.
`headword` · `pos` · `meaning` · 파생어 · 유의어 · 반의어 · 출처 문장은 불러올 때 보이는 시트에서 읽는다.
`_meta` 는 **사람이 고칠 수 없고 고칠 이유도 없는 기술 필드**(surface · properNoun · passageNo 등)만 맡는다.

그래서 `_meta` 에 행 전체를 JSON 으로 넣는 방법은 쓰지 않았다. 그렇게 하면 손으로 고친 내용이
숨김 시트의 옛 값에 덮여 조용히 되돌아간다.

### 반의어 confidence 를 안 담은 이유

`confidence` 는 `trimAntonyms` 가 반의어 비율을 30~50% 로 맞출 때만 읽는다. 그 함수는 생성 단계에서만
돌고 불러오기 경로에서는 절대 돌지 않는다. 아무도 읽지 않을 값을 위해 열을 하나 늘릴 이유가 없다.
따라서 왕복 후 `antonyms[].confidence` 는 0 이 된다 — 체크리스트의 "rows 가 원본과 같다"는
**시험지가 실제로 읽는 필드 기준**으로 읽어야 한다.

### 버전 번호 대신 열 이름

`_meta` 에 포맷 버전을 박아두는 대신 **첫 행의 열 이름을 보고 읽는다.** 나중에 열이 늘어도
그전에 내보낸 파일은 있는 열만 읽히고 없는 열은 비게 되어 자연히 호환된다.
버전 번호를 두면 올릴 때마다 마이그레이션 분기가 쌓인다.

### 읽을 때 주의할 점 (다음 작업용)

ExcelJS 의 `row.values` 는 **성긴 배열**이다. 빈 셀이 `undefined` 로 남아 열 위치가 어긋날 수 있다.
불러오기에서는 `row.getCell(n)` 으로 위치를 지정해 읽는다.

### 역추적에서 걸린 것 두 가지 (2026-08-29)

**낱말 경계를 안 봐서 단어가 잘렸다.** `develop` 를 `"Developing countries…"` 에서 찾을 때
`indexOf` 만 쓰면 0번 자리에서 걸려 `"Develop"` 을 잘라 온다. 지문에 없는 형태를 정답으로
내놓는 셈이라, 형태를 못 찾는 것보다 나쁘다. 양끝이 낱말 경계인 자리만 인정하도록 고쳤다.
그러면 정확 일치에 실패하고 굴절 비교로 내려가 `"Developing"` 을 제대로 찾는다.

**묵음 e 동사가 통째로 빠졌다.** `stripInflection('immersed')` 는 `ed` 를 떼어 `immers` 를 주지만
`immerse` 는 그대로 `immerse` 다. 키가 어긋나 못 찾는다. create/created, produce/produced,
achieve/achieved… 학술 지문에 흔한 부류가 전부 여기 걸린다.

그래서 표제어 쪽 열쇠에서 끝 `e` 를 뗀 것도 후보로 함께 본다. 단 **남는 길이가 4자 미만이면
만들지 않는다** — `rate` 를 `rat` 으로 줄이면 문장 속 `rats` 와 잘못 묶인다.
`duplicates.js` 의 `MIN_STEM` 과 같은 이유이고 같은 값이다. 여기서 오탐은 지문에 없는 형태를
정답으로 만들기 때문에 미탐보다 훨씬 나쁘다.

### 어구는 첫 낱말이 변하면 포기한다

`lose track of` 를 `He lost track of time.` 에서 찾는 것은 규칙으로 안 된다.
`stripInflection('lost')` 는 `lost` 그대로다 (불규칙). 창을 밀며 굴절 열쇠를 견주는 방식으로는
잡히지 않는다. 억지로 맞추려면 첫 낱말만 느슨하게 보는 규칙이 필요한데, 그러면
`take care of` / `took part in` 류가 잘못 묶인다. 못 찾았다고 알리는 쪽을 택했다.

### 옛 파일에서 passageNo 를 되찾는 조건

출처 칸은 `passageLabel ?? passageNo` 라서 파일이 하나면 `"1"`, 여럿이면 `"11강-1"` 이다.
숫자로만 이루어졌을 때만 지문 번호로 되돌린다. `"11강-1"` 은 `passageNo` 를 비워 두는데,
어차피 `_meta` 없는 파일에는 `_passages` 도 없으므로 PART IV 는 문장 하나만 쓴다.
